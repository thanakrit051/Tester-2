# บันทึกการเปลี่ยนแปลง

เลขเวอร์ชันคือ `APP_VERSION` ใน [`js/version.js`](js/version.js) — ต้องตรงกับ
`VERSION` ใน `sw.js` และ `version` ใน `package.json` เสมอ (`tools/check-sw.mjs` ตรวจให้)

`SERVER_VERSION` ของโค้ดฝั่งชีตเดินแยกกัน เพราะไม่จำเป็นต้องอัปเดตพร้อมกัน

---

## 3.8.0 — 11 ก.ย. 2569

ต้องอัปโค้ดในชีตด้วย (`SERVER_VERSION` 2.13.2 → 2.14.0) และบวก `NEEDS_SERVER` เป็น 2.14.0
เพราะคะแนนสอบซ่อมเป็นข้อมูลรูปแบบใหม่ — ชีตรุ่นเก่าอ่าน `R15/6` ไม่ออก จะนับเป็นยังไม่ตรวจแล้วคะแนนสรุปหาย
ระหว่างที่ยังไม่อัป แอปขึ้นแถบเตือนสีแดง และปุ่มสอบซ่อมไม่ยอมบันทึก (อย่างอื่นใช้ได้ตามปกติ)

### ใหม่: บันทึกคะแนนสอบซ่อม (สอบเก็บคะแนน · กลางภาค)

- หน้ากรอกคะแนนมีปุ่มที่ 3 **สอบซ่อม** ต่อจาก สอบแล้ว / ยังไม่สอบ — กดแล้วกรอกคะแนนสอบซ่อม
  ระบบเก็บคะแนนครั้งแรกไว้ให้ ปุ่มกลายเป็นสีม่วง **ซ่อมแล้ว · เดิม 6** และช่องคะแนนขอบม่วง
- ช่องคะแนนครั้งแรกในกล่องแก้ได้ เผื่อเผลอพิมพ์คะแนนซ่อมทับช่องไปก่อนกดปุ่ม ·
  มีปุ่มยกเลิกสอบซ่อม (กลับไปใช้คะแนนครั้งแรก) และปุ่มเลิกทำใน toast
- **คิดคะแนนด้วยคะแนนซ่อมตามที่กรอกจริง** ไม่ตัดเพดานที่เกณฑ์ผ่าน · ซ่อมผ่านแล้วหลุดจากรายชื่อคนไม่ผ่าน
  และธงหมายเหตุในชีตเอง · ซ่อมแล้วยังไม่ถึงเกณฑ์ยังขึ้นแดงเหมือนเดิม
- ช่องที่ซ่อมแล้ว กดปุ่มสอบแล้ว/ยังไม่สอบ จะเปิดกล่องสอบซ่อมแทนการเขียนทับทันที — กันกดพลาดทีเดียวแล้วคะแนนหายทั้งคู่
- พิมพ์แก้ในช่องคะแนนของคนที่ซ่อมแล้ว = แก้คะแนนซ่อม คะแนนครั้งแรกอยู่ที่เดิม
- เก็บในช่องเดียวกับคะแนน: `R15/6` = ซ่อมได้ 15 · ครั้งแรก 6 (`R15/x` = ครั้งแรกขาดสอบ) แบบเดียวกับ `L8`
  คิวออฟไลน์ เลิกทำ แคช จึงใช้ของเดิมได้หมด · วางจาก Excel ด้วยรูปแบบนี้ได้ · แท็บวิธีใช้ในชีตอธิบายไว้
- รายงาน: กลุ่มไม่ผ่านเกณฑ์บอกว่าซ่อมผ่านแล้วกี่คน และชิปของคนที่ซ่อมแล้วยังไม่ผ่านขึ้นคำว่า "ซ่อม"
- หน้านักเรียน: ป้าย **สอบซ่อม · ครั้งแรก 6/20** ใต้ชื่อข้อสอบ
- เรื่องล็อกอินไม่อยู่ใน `FEATURES` — แถบเตือนอ่านรายการนั้นว่า "จะไม่ถูกบันทึกลงชีต" ซึ่งไม่จริงกับการล็อกอิน
  (หน้าตรวจสภาพเตือนเรื่องล็อกอินแยกไว้แล้ว)

### แก้: เปิดเว็บใหม่แล้วต้องเข้าสู่ระบบด้วย Google ทุกครั้ง

คอมมิต 9b0a62b พยายามแก้ด้วยการขอ ID token ใหม่เงียบ ๆ ตอนเปิดแอป แต่ยังพึ่งให้ Google
ล็อกอินให้เอง ซึ่งล้มในกรณีที่เจอบ่อยที่สุด — Safari/iPhone (บล็อกคุกกี้ที่ต้องใช้) · มีหลายบัญชี Google
ในเบราว์เซอร์ · Google พักการล็อกอินอัตโนมัติ 10 นาทีหลังครั้งก่อน · และแอปรอแค่ 4 วินาที

- **ชีตออกบัตรผ่านอายุ 30 วัน** หลังตรวจ ID token ผ่าน (`issueSession_` · `verifySession_` ใน `04_Api.gs`)
  เซ็นด้วย HMAC-SHA256 จาก `apiKey` ตรวจในเครื่องไม่ต้องยิงไปถาม Google (คำขอเร็วขึ้นด้วย)
  และออกใบใหม่ให้เมื่อใบเดิมเก่ากว่า 1 วัน = เปิดแอปอย่างน้อยเดือนละครั้งไม่ต้องล็อกอินอีก
  - เอาอีเมลออกจาก `allowed_emails` → คนนั้นหลุดทันที (ตรวจทุกคำขอ)
  - เมนูสร้างรหัสลับใหม่ → ทุกเครื่องต้องเข้าสู่ระบบใหม่ (ใช้ตอนทำมือถือหายได้)
- **แก้บั๊กที่ทำให้การต่อเซสชันเงียบ ๆ พังถาวร** — ตอนเปิดแอปแล้วชีตตอบ `AUTH` แอปเรียก
  `auth.signOut()` ซึ่งลบ profile และปิด auto-select ของ Google หลังจากครั้งนั้น `restoreSession`
  จึงไม่ทำงานอีกเลย เปลี่ยนเป็น `auth.expire()` ที่ทิ้งเฉพาะ token ที่ใช้ไม่ได้
- ถ้า token ในเครื่องยังดูไม่หมดอายุแต่ชีตไม่รับ `restoreSession` จะตอบ true ทันทีโดยไม่ได้ขออะไรใหม่
  แล้วยิงซ้ำก็โดนปฏิเสธซ้ำ — ตอนนี้ `api.call` ทิ้งของที่ถูกปฏิเสธก่อนเสมอ
- ตัวตั้งเวลาต่อ ID token รายชั่วโมงหยุดทำงานเมื่อมีบัตรผ่านแล้ว
  (เดิมกล่อง One Tap เด้งกวนกลางจอทุกชั่วโมงถ้า Google ล็อกอินให้เองไม่ได้)
- หน้าตรวจสภาพบอกว่าจำการเข้าสู่ระบบไว้ถึงวันไหน หรือเตือนว่าโค้ดในชีตยังไม่ออกบัตรให้

### เทส

- `test/session.mjs` ใหม่ 55 ข้อ เข้า preflight — บัตรปลอม/แก้ลายเซ็น/สลับอีเมล/ต่ออายุเอง/หมดอายุ
  ต้องไม่ผ่าน · ถอนสิทธิ์และสร้างรหัสลับใหม่ต้องมีผลทันที · หน้าเว็บเก็บ ต่ออายุ และทิ้งบัตรถูกจังหวะ
- `test/parity.mjs` — สุ่มคะแนนสอบซ่อมเข้าชุดเทียบสูตร 2 ฝั่ง · อ่านค่าสอบซ่อม 8 รูปแบบต้องได้เหมือนกันทั้ง 2 ฝั่ง ·
  คนที่ซ่อมผ่าน/ไม่ผ่าน/ครั้งแรกขาดสอบ ต้องได้คะแนนและธงไม่ผ่านถูกต้อง

## 3.7.2 — 7 ก.ย. 2569

ต้องอัปโค้ดในชีตด้วย (`SERVER_VERSION` 2.13.1 → 2.13.2) — เป็นบั๊กที่ทำข้อมูลหาย
ไม่ใช่แค่แสดงผลเพี้ยน จึงบวก `NEEDS_SERVER` ให้แอปขึ้นแถบเตือนจนกว่าจะ Deploy ใหม่

### แก้บั๊ก: ห้องที่รายชื่อมีแถวว่างคั่นกลาง คะแนนสรุปลงผิดคน และแก้รายชื่อทีเดียวคะแนนหาย

อาการที่ครูเจอ: ตั้งเกณฑ์ผ่านให้ข้อสอบแล้ว **บางห้องเหมือนไม่มีอะไรเกิดขึ้น** —
คนที่สอบตกไม่ติดธง คนที่ผ่านกลับติด และช่องคะแนนสรุปของคนท้ายห้องว่างเปล่า

ต้นเหตุคือโค้ด 2 จุดที่เดาว่า "รายชื่อนักเรียนอยู่ติดกันไล่ลงไปจากแถว 8"
ซึ่งไม่จริงเมื่อครูลบชื่อคนที่ย้ายออกแต่ไม่ได้ลบแถวทิ้ง — ตัวอ่านข้ามแถวว่างนั้นถูกแล้ว
แต่ตัวเขียนไล่เขียนติดกันรวดเดียว ทุกอย่างใต้แถวว่างจึงเลื่อนขึ้นไปสวมของคนข้างบน
ห้องที่ไม่มีแถวว่างจึงปกติดี — เลยดูเหมือนบั๊ก "เฉพาะบางห้อง"

- **`recalcClass_`** — คะแนนสรุป · เกรด · % เวลาเรียน · ธง "ไม่ผ่านเกณฑ์"
  ลงผิดแถวทั้งบล็อก และช่องของคนสุดท้ายค้างว่าง
  แยกการจัดแถวออกมาเป็น `summaryBlock_` ที่เขียนตามเลขแถวจริงของแต่ละคน
  แถวว่างถูกล้างเป็นช่องว่าง ไม่ใช่ถูกยัดของคนถัดไป (ยังเขียนรวดเดียวเหมือนเดิม ไม่ช้าลง)

- **`setStudents_`** — ร้ายแรงกว่า: ตอนแก้รายชื่อ ระบบเก็บคะแนนเดิมไว้ตามลำดับในรายชื่อ
  แทนที่จะเป็นแถวจริง **คะแนนของทุกคนใต้แถวว่างจึงหายไปทั้งหมด** (เทสยืนยันว่าโค้ดเดิม
  เหลือคะแนนแค่คนแรกคนเดียว) แก้ให้หยิบจากแถวจริง และล้างช่วงให้ครอบถึงแถวว่างท้ายรายชื่อ
  พร้อมขยายชีตก่อนล้างเพื่อไม่ให้ `getRange` โยนขอบชีตเมื่อรายชื่อใหม่ยาวกว่าเดิม

### แก้บั๊ก: เกณฑ์ผ่านที่เพิ่งตั้งหายจากจอเมื่อคำสั่งอ่านแซงคำสั่งเขียน

`withPending` เอาของที่ค้างคิวมาทับข้อมูลจากชีตเฉพาะ `setCells` เท่านั้น
คำสั่งโครงสร้าง (`addColumn` · `updateColumn` · `deleteColumn`) ไม่ถูกทับคืน
รายการที่เพิ่งเพิ่ม หรือเกณฑ์ผ่าน/คะแนนเต็มที่เพิ่งแก้ จึงหายจากจอกลางคัน
แล้วโผล่กลับมาเองตอนคิวส่งสำเร็จ — ครูจะนึกว่าตั้งเกณฑ์ผ่านไม่ได้ แล้วกดซ้ำ
ตอนนี้ทับคืนครบทุกคำสั่ง รวมถึงตอนโหลดห้องอื่นเผื่อไว้เบื้องหลัง (`prefetchClasses`)

### แก้คำเตือนเวอร์ชันที่บอกผิด

- `NEEDS_SERVER` ค้างอยู่ที่ `2.8.0` ตั้งแต่ก่อนมีเกณฑ์ผ่าน ชีตรุ่น 2.8–2.12
  จึงผ่านด่านตรวจทั้งที่รับค่า `pass` ไม่ได้ — บวกเป็น `2.13.2` และเพิ่มรายการใน `FEATURES`
- หน้าตรวจสภาพเทียบเวอร์ชันชีตกับ `NEEDS_SERVER` (ค่า **ต่ำสุด**) แล้วสรุปว่า
  "โค้ดในชีตใหม่กว่าหน้าเว็บ" ทั้งที่ติดตั้งถูกต้องครบทุกอย่าง
  เพิ่ม `SERVER_BUILT_FOR` (เวอร์ชันชีตที่มาคู่กับหน้าเว็บรุ่นนี้) แล้วเทียบกับค่านั้นแทน
- แถบเตือนบนสุดฝังคำว่า "ส่งช้า" ไว้ตายตัว ทั้งที่ฟีเจอร์ที่พังจริงเปลี่ยนไปตามเวอร์ชัน
  เปลี่ยนเป็นบอกชื่อฟีเจอร์ที่หายจริงตามเวอร์ชันที่เจอ

### การ์ด "ส่งไม่ผ่านหลายรอบ" บอกสาเหตุที่ชีตตอบกลับมา

`flush()` ทิ้งข้อความ error ตอนเอางานกลับเข้าคิว หน้าตรวจสภาพจึงบอกได้แค่
"ส่งไม่ผ่าน" โดยไม่มีทางรู้ว่าติดตรงไหน ตอนนี้เก็บไว้เป็น `lastError` แล้วแสดงบนการ์ด

### เทส

- `test/classsheet.mjs` 31 → 41 ข้อ — ครอบห้องที่รายชื่อมีแถวว่างคั่นกลาง
  ทั้งการจัดบล็อกสรุปและการแก้รายชื่อ (รันกับโค้ดเดิมแล้วไม่ผ่าน 6 ข้อ)
- `test/state.mjs` 12 → 17 ข้อ — คำสั่งโครงสร้างที่ค้างคิวต้องไม่ถูกข้อมูลจากชีตทับหาย

## 3.7.1 — 5 ก.ย. 2569

แก้เฉพาะฝั่งชีต (`SERVER_VERSION` 2.13.0 → 2.13.1) — หน้าเว็บไม่เปลี่ยน ไม่ต้อง push อะไรใหม่

### แก้บั๊ก: ตั้งวันสอบกลางภาคแล้วขึ้นว่าบันทึก แต่ช่องวันที่ว่างเปล่า

`setValue('2026-10-01')` Google Sheets ตีความเหมือนคนพิมพ์เอง แล้วแปลงช่องนั้น
เป็นชนิด "วันที่" ให้เอง พออ่านกลับด้วย `getValues()` จึงได้ `Date` ไม่ใช่ข้อความ
และ JSON แปลงต่อเป็น `2026-09-30T17:00:00.000Z` (มีเวลากับเขตเวลาติดมา)

ฝั่งเว็บตรวจรูปแบบด้วย `/^\d{4}-\d{2}-\d{2}$/` จึงไม่ผ่าน · `<input type="date">`
ก็ไม่รับค่าที่มีเวลาติดมา ช่องเลยว่าง และแถบเตือน "ยังไม่ได้ตั้งวันสอบกลางภาค"
ยังค้างอยู่ — **ทั้งที่ค่าลงชีตไปแล้วจริง ๆ** ข้อความ "บันทึกแล้ว" จึงไม่ได้โกหก
แค่ของที่ส่งกลับมาคนละชนิดกับที่ส่งไป

แก้ 2 ชั้น
- **ตอนอ่าน** — เจอ `Date` ในค่าตั้งค่าเมื่อไหร่ แปลงกลับเป็น `YYYY-MM-DD`
  ตามเขตเวลาของสเปรดชีต (ถ้าใช้ UTC วันจะเพี้ยนไป 1 วันสำหรับไทย UTC+7)
  ทำให้ค่าที่ค้างอยู่ในชีตอยู่แล้วกลับมาแสดงถูกทันที ไม่ต้องกรอกใหม่
- **ตอนเขียน** — บังคับรูปแบบเซลล์เป็นข้อความก่อนเขียนค่าที่หน้าตาเป็นวันที่
  เพื่อไม่ให้ Sheets แปลงชนิดอีก

เพิ่ม [`test/config.mjs`](test/config.mjs) เข้า preflight (12 ข้อ) คุมทั้งการแปลงกลับ
การไม่ไปยุ่งกับค่าชนิดอื่น (ตัวเลข · ศูนย์ · บูลีน · ข้อความ) และเรื่องเขตเวลา

## 3.7.0 — 5 ก.ย. 2569

ต้องอัปโค้ดในชีตด้วย (`SERVER_VERSION` 2.12.0 → 2.13.0) แล้วกดเมนู
**📗 AssignCheck → 🚀 ติดตั้ง / ซ่อมแซมโครงสร้าง** หนึ่งครั้งเพื่อเพิ่มแถวใหม่ให้ทุกห้อง

### เกณฑ์ผ่านรายข้อสอบ

เดิมข้อสอบมีแค่ "เต็มเท่าไหร่" กับ "ได้เท่าไหร่" ไม่มีเส้นว่าเท่าไหร่เรียกว่าผ่าน
ครูจึงต้องไล่ดูเองว่าใครต้องเรียกมาซ่อม

ตอนนี้ตั้งเกณฑ์ได้ทีละรายการ — ในแอปมีช่อง "เกณฑ์ผ่าน" ตอนเพิ่ม/แก้รายการ
หรือพิมพ์ลงแถวที่ 7 ของชีตตรง ๆ ก็ได้ · เว้นว่าง = ไม่ตรวจรายการนั้น
และมีค่าตั้งต้นเป็น % ให้ตั้งครั้งเดียวได้ที่ `pass_default_pct` ใน ⚙️ ตั้งค่า
(เว้นว่างไว้ = ตรวจเฉพาะรายการที่ตั้งเกณฑ์เอง — ของเดิมจึงไม่มีอะไรเปลี่ยน)

เกณฑ์เก็บเป็น **คะแนนดิบ** ไม่ใช่ % เพราะครูอ่านเทียบกับคะแนนเต็มในแถวเหนือมันได้ทันที
และไม่ต้องเดาเรื่องปัดเศษตอนคะแนนเต็มเป็นเลขคี่ (เต็ม 15 · ผ่าน 50% = 7.5)

เห็นผลได้ 4 ที่
- **หน้ากรอกคะแนน** — ช่องที่ต่ำกว่าเกณฑ์ขึ้นสีแดงทันทีตอนพิมพ์ และบอกใต้ช่องว่าผ่านที่เท่าไหร่
- **หน้ารายงาน** — การ์ด "ไม่ผ่านเกณฑ์" แยกตามข้อสอบ ไม่ใช่แยกตามคน
  เพราะการซ่อมทำเป็นรอบตามชิ้นงาน ครูต้องการรายชื่อไปเรียกทีเดียวทั้งกลุ่ม
- **คอลัมน์หมายเหตุในชีต** — ธง `ไม่ผ่านเกณฑ์ n รายการ (ชื่อรายการ…)` ต่อจาก `มส` เดิม
- **หน้านักเรียน** — ป้ายผ่าน/ไม่ผ่านใต้ชื่อรายการ และบอกเกณฑ์ในหน้ารายละเอียด

กติกา 2 ข้อที่ตั้งใจให้เป็นแบบนี้
- ช่องที่ครู**ยังไม่ตรวจ** ไม่ถือว่าไม่ผ่าน ไม่งั้นวันแรกที่สร้างข้อสอบจะติดธงกันทั้งห้อง
- `x` (ไม่ส่ง / ยังไม่ได้สอบ) นับเป็น 0 = ไม่ผ่าน ซึ่งตรงกับความจริง
- เช็คชื่อไม่มีเกณฑ์รายคาบ เพราะมีเกณฑ์เวลาเรียนรวม (`att_min_pct`) อยู่แล้ว

### แท็บห้องเรียนมีแถวที่ 7 เพิ่มมา

ข้อมูลนักเรียนเลื่อนจากแถว 7 ไปแถว 8 · ชีตที่สร้างไว้ก่อนหน้านี้**ยังใช้ได้ตามปกติ**
ตัวอ่านรองรับทั้ง 2 โครง (คำสั่งอ่านไม่จับ lock จะไปแทรกแถวเองไม่ได้)
ส่วนคำสั่งที่เขียนจะอัปโครงห้องนั้นให้เองก่อนเสมอ กดซ่อมแซมโครงสร้างคือกวาดให้ครบทีเดียว

เพิ่มเทสคุมการอัปโครงไว้ด้วย — เป็นฟังก์ชันที่แทรกแถวกลางตาราง ถ้าพลาดคะแนนทั้งห้อง
จะเลื่อนไปสวมคนอื่นโดยไม่มีอะไรเตือน · `test/classsheet.mjs` ขยายจาก 15 เป็น 31 ข้อ
และ `test/parity.mjs` เทียบเพิ่มเป็น 43,283 ค่า (ครอบเกณฑ์ผ่านทั้ง 2 ฝั่ง)

## 3.6.0 — 5 ก.ย. 2569

ต้องอัปเดตโค้ดในชีตด้วย (`SERVER_VERSION` 2.11.0 → 2.12.0) — ตัวที่ทำให้เร็วขึ้น
อยู่ฝั่งชีตเกือบทั้งหมด อัปแค่หน้าเว็บจะไม่รู้สึกอะไรเลย

### ไม่ต้องกดเข้าสู่ระบบใหม่ทุกครั้งที่เปิดแอป

ID token ของ Google อายุแค่ 1 ชั่วโมง ของเดิมมีแต่ตัวตั้งเวลาต่ออายุ ซึ่งทำงาน
เฉพาะตอนแท็บเปิดค้างอยู่ พอปิดแท็บแล้วกลับมาเปิดใหม่วันรุ่งขึ้น token หมดอายุไปแล้ว
`conn.ready` จึงเป็น false = เด้งไปหน้าเข้าสู่ระบบทุกเช้า ทั้งที่บัญชี Google
ในเบราว์เซอร์ยังล็อกอินอยู่และเคยกดอนุญาตไปแล้ว

ตอนนี้ตอนเปิดแอปจะขอ token ใหม่เงียบ ๆ ก่อน (ไม่มีอะไรเด้งขึ้นมา เพราะเคยอนุญาตแล้ว)
ได้ก็เข้าใช้งานต่อเลย ไม่ได้ค่อยไปหน้าเข้าสู่ระบบตามเดิม · เพิ่มอีก 2 จุด
- กลับมาที่แท็บที่พับไว้นาน ๆ → ต่ออายุให้ทันที (เบราว์เซอร์หยุด timer ของแท็บที่ไม่ได้ใช้
  ตัวตั้งเวลาเดิมจึงพึ่งไม่ได้ โดยเฉพาะบนมือถือ)
- เซสชันหมดอายุพอดีตอนกำลังยิงคำสั่ง → ขอใหม่แล้วลองอีกรอบ ก่อนจะไล่ให้ล็อกอิน

แก้บั๊กที่เกี่ยวกัน: `initialize()` ของ Google เรียกได้ครั้งเดียว ของเดิมจึงล็อก
callback ของคนที่เรียกก่อนไว้ตลอด ถ้าการต่ออายุเงียบ ๆ ทำงานก่อน ปุ่ม
"เข้าสู่ระบบด้วย Google" ที่กดทีหลังจะบันทึก token ได้แต่ `onSignedIn` ไม่ถูกเรียก —
หน้าเข้าสู่ระบบค้างอยู่กับที่ทั้งที่ล็อกอินผ่านแล้ว

### ทำให้ฝั่งชีตเร็วขึ้น (ลดจำนวนครั้งที่คุยกับ Google)

Apps Script คิดเวลาตาม **จำนวนครั้งที่เรียก** ไม่ใช่ปริมาณข้อมูล วัดจริงพบว่าคำขอ
ที่เบาที่สุด (`?api=1`) ใช้เวลา 1.7–2.3 วินาที ซึ่งเป็นพื้นที่กดให้ต่ำกว่านี้ไม่ได้
แต่คำสั่งจริงเรียกชีตซ้ำเกินจำเป็นมาก จึงไล่เก็บทีละจุด

- **`recalcClass_`** — ต้นเหตุหลักที่ครูรู้สึกว่า "กรอกคะแนนแล้วรอ" เพราะมันตามหลัง
  การกรอกทุกครั้ง ของเดิมเรียก `ensureColumn_` ครบทั้ง 12 คอลัมน์สรุปทุกรอบ
  และแต่ละครั้งอ่านหัวคอลัมน์ทั้งแท็บใหม่หมดแล้วเขียนทับชื่อ/คะแนนเต็มซ้ำของเดิม
  รวม ~80 รอบต่อการคำนวณ 1 ครั้ง ทั้งที่แทบทุกครั้งไม่มีคอลัมน์ไหนขาดเลย
  ตอนนี้เช็คก่อนแล้วเติมเฉพาะตัวที่ขาด และเขียนคะแนนสรุปรวดเดียวแทน 12 รอบ
- **`readClassBySheet_`** — คำสั่งที่ถูกเรียกบ่อยที่สุด (เปิดแอป · สลับห้อง ·
  หลังบันทึกทุกครั้ง) เดิมอ่านทีละส่วน 7–9 รอบ ตอนนี้ขอตารางทั้งผืนทีเดียวเหลือ 2 รอบ
- **`sheetForClass_`** — เดิมไล่เปิดทุกแท็บแล้วอ่านเซลล์ทีละใบ ครูที่มี 8 ห้อง
  เสียเวลา 8 รอบต่อ *ทุกคำสั่งที่แตะห้องเรียน* ตอนนี้ถามสารบัญ 🏫 ห้องเรียน ก่อน
  (ยังมีการไล่ทุกแท็บไว้เป็นทางสำรอง เผื่อสารบัญเพี้ยนหรือครูเปลี่ยนชื่อแท็บเอง)
- **`getConfig_` / `listClasses_`** — คำขอเดียวเรียกซ้ำ 3–5 ครั้ง ทั้งที่ค่าเปลี่ยนไม่ได้
  ระหว่างคำขอเดียว ตอนนี้จำไว้ตลอดคำขอ (จงใจไม่เก็บข้ามคำขอ ครูแก้ค่าในชีตแล้วต้องเห็นผลทันที)
- **`columnsOf_`** — แถว 4-6 อยู่ติดกัน ขอทีเดียวได้ ไม่ต้องขอทีละแถว

### แก้บั๊ก: แถวว่างคั่นกลางรายชื่อทำให้คะแนนไปโผล่ผิดคน

เจอระหว่างเขียนเทสให้ตัวอ่านแท็บห้องเรียน ของเดิมนับแถวข้อมูลแบบต่อเนื่องจาก
`R_DATA` ทั้งที่รายชื่ออาจไม่ติดกัน (แถวว่างคั่นกลาง) คะแนนของคนหลังแถวว่าง
จึงเลื่อนไปสวมของคนอื่นเงียบ ๆ — บนจอดูปกติทุกอย่าง

เพิ่ม [`test/classsheet.mjs`](test/classsheet.mjs) เข้า preflight (15 ข้อ) ครอบทั้ง
ความถูกต้องและเพดานจำนวนครั้งที่เรียกชีต ชั้นนี้เมื่อก่อนไม่มีเทสเลยสักข้อ

## 3.5.1 — 5 ก.ย. 2569

ระหว่างที่หน้าติดตั้งกำลังถาม `oauth_client_id` จากชีต (Apps Script ตอบ 2-10 วินาที)
มันขึ้นกล่องเหลือง "ยังไม่ได้เปิดใช้การเข้าสู่ระบบด้วย Google" ไปก่อน
ทั้งที่ยังไม่รู้ผล — ตอนนี้ขึ้น "กำลังอ่านการตั้งค่าจากชีต…" แทน

แก้ README: ใส่ `oauth_client_id` แล้ว**ไม่ต้อง Deploy ใหม่** (ฝั่งชีตอ่านสดทุกครั้ง)
และเมนู OAuth ของ Google Cloud ย้ายไปอยู่ใต้ชื่อ Google Auth Platform แล้ว

## 3.5.0 — 5 ก.ย. 2569

### เครื่องใหม่ไม่ต้องคัดลอกลิงก์ Apps Script มาวางอีกแล้ว

เดิมทุกเครื่องใหม่ต้องผ่านขั้น "วาง Web app URL" ก่อนถึงจะเห็นปุ่มเข้าสู่ระบบ
ด้วย Google ซึ่งแปลว่าต้องไปเปิด Apps Script คัดลอกลิงก์มาทุกครั้ง

ตอนนี้มี [`config.js`](config.js) ที่รากโปรเจกต์เก็บลิงก์ตั้งต้นไว้ (แบบเดียวกับ
`docs/config.js` ของหน้านักเรียน) เปิดเว็บบนเครื่องใหม่แล้วข้ามไปหน้าเข้าสู่ระบบเลย
ค่าที่กรอกเองในเครื่องยังชนะค่าในไฟล์นี้เสมอ

ลิงก์ Web App ไม่ใช่ความลับอยู่แล้ว (ใครยิงก็ได้ แต่ถูกปฏิเสธถ้าไม่มีสิทธิ์ —
ตัวที่กันคือบัญชี Google กับรหัสลับ ซึ่งไม่ได้อยู่ในไฟล์นี้)

### แก้ปุ่ม "‹ เปลี่ยน URL" ที่กดแล้วไม่ไปไหน

หน้าติดตั้งตัดสินว่าอยู่ขั้นไหนจาก `ui.info` ซึ่งมีค่าเฉพาะรอบที่เพิ่งกรอก URL เอง
ครูที่รีเฟรชหน้าแล้วกด "‹ เปลี่ยน URL" จึงถูกดีดกลับมาขั้นเข้าสู่ระบบทันที
แก้ URL ไม่ได้เลยจนกว่าจะกดตัดการเชื่อมต่อทิ้ง ตอนนี้ใช้ธงแยกต่างหาก

## 3.4.0 — 5 ก.ย. 2569

### สลับมาใช้บัญชี Google ได้โดยไม่ต้องตัดการเชื่อมต่อ

ตัวเลือก "เข้าสู่ระบบด้วย Google" เคยมีอยู่ที่เดียวคือหน้าติดตั้งครั้งแรก ซึ่งอ่านค่า
`oauth_client_id` จากชีตตอนกรอก URL ครั้งเดียวแล้วเก็บไว้ในเครื่อง
ครูที่เริ่มด้วยรหัสลับไปก่อน แล้วเพิ่งมาเปิด Google Sign-In ทีหลัง จึงไม่มีทาง
สลับเลย นอกจากกด "ตัดการเชื่อมต่อ" ทิ้งทั้งหมดแล้วตั้งใหม่ (ซึ่งล้างแคชทั้งเครื่องด้วย)

ตอนนี้ **⚙️ ตั้งค่า → บัญชีและการเชื่อมต่อ → 🔐 เข้าสู่ระบบด้วย Google**
จะไปถาม `oauth_client_id` จากชีตสด ๆ อีกครั้งแล้วเปิดหน้าล็อกอินให้เลย
รหัสลับเดิมยังอยู่ ใช้เป็นตัวสำรองได้ต่อ · ปุ่มจะโผล่เฉพาะตอนที่ยังไม่ได้ล็อกอิน
ด้วย Google และเปิดจากโฮสต์ภายนอก (เปิดผ่าน `/exec` Google ล็อกอินให้อยู่แล้ว)

## 3.3.0 — 4 ก.ย. 2569

### เปิดห้องเรียนแล้วไม่ต้องนั่งรอ

เจอตอนย้ายมาเปิดแอปผ่าน GitHub Pages: กดอะไรก็ต้องรอข้อมูลนานทุกครั้ง
วัดจริงแล้วคำสั่งเดียวใช้เวลา 2–3 วินาที บางครั้งถึง 10 (Apps Script เด้ง
redirect ไป `googleusercontent.com` อีกทอด = 2 รอบเน็ตต่อ 1 คำสั่ง)
กดให้เร็วกว่านี้ไม่ได้ จึงแก้ที่ "ทำให้ครูไม่ต้องรอมันบ่อย ๆ" แทน 3 ทาง

**1. ฝั่งชีตเลิกจับ lock ตอนอ่าน** — ของเดิมทุกคำขอไปคว้า lock ตัวเดียวกันหมด
เวลาครูกรอกคะแนนแล้วกดเปิดห้องอื่นต่อทันที คำสั่ง "อ่าน" ต้องยืนรอคำสั่ง
"เขียน" ที่แอปส่งเบื้องหลังให้เสร็จก่อน = รอ 2 เด้ง 4–6 วินาที ทั้งที่การอ่าน
ไม่ได้แก้อะไรในชีตเลย ตอนนี้ `ping` / `bootstrap` / `getClass` (และ `batch`
ที่มีแต่คำสั่งอ่าน) ข้ามคิวไปได้ — **ต้อง Deploy โค้ดฝั่งชีตใหม่ถึงจะมีผล**

**2. โหลดห้องที่เหลือเผื่อไว้เบื้องหลัง** — หลังเปิดแอปเสร็จ แอปจะดึงห้องที่ยัง
ไม่มีในเครื่องมาเก็บไว้เงียบ ๆ (ไม่เกิน 4 ห้อง และไม่ให้แคชกินพื้นที่จนคิวงาน
ไม่มีที่อยู่) กดสลับห้องครั้งต่อไปจึงขึ้นทันทีแล้วค่อยอัปเดตทีหลัง

**3. กดแล้วเปลี่ยนหน้าทันที** — ของเดิมกดการ์ดห้องเรียนแล้ว `await` รอเน็ตก่อน
ถึงจะเปลี่ยนหน้า ครูจึงเจอจอนิ่ง 2–10 วินาทีทั้งที่แคชพร้อมวาดตั้งแต่แรก
และหน้าที่ยังไม่มีข้อมูลขึ้นว่า "กำลังโหลดห้องเรียน…" แทน "ยังไม่ได้เลือก
ห้องเรียน" ซึ่งอ่านแล้วเหมือนแอปพัง

### กันของหายจากการที่คำสั่งอ่านแซงคำสั่งเขียน

ผลข้างเคียงของข้อ 1 คือคำสั่งอ่านอาจแซงคำสั่งเขียนที่ยังส่งไม่ถึง แล้วเอาค่าเก่า
มาทับจอ — ช่องที่ครูเพิ่งกดจะหายไปต่อหน้าทั้งที่ยังอยู่ในคิว ตอนนี้ข้อมูลที่ดึงมา
จากชีตจะถูกทับด้วยค่าที่ยังค้างคิวเสมอก่อนขึ้นจอและก่อนเก็บลงแคช
มีเทสต์คุมไว้ที่ [`test/state.mjs`](test/state.mjs) (`node tools/preflight.mjs`)

## 3.2.0 — 4 ก.ย. 2569

### ปุ่มย้อนกลับของเครื่องใช้ได้แล้ว

ก่อนหน้านี้แต่ละหน้าในแอปไม่มีที่อยู่ของตัวเอง ครูที่อยู่หน้า "งาน/คะแนน"
แล้วกดปุ่มย้อนกลับจะ**ออกจากแอปทันที** (ถ้าติดตั้งเป็นแอปบนมือถือคือปิดไปเลย)
และส่งลิงก์เจาะจงหน้าให้เพื่อนครูไม่ได้

ตอนนี้ทุกหน้ามีที่อยู่ (`#att`, `#work`, …) ย้อนกลับ/ไปข้างหน้าได้จริง
ลิงก์ที่ชี้ไปหน้าซึ่งต้องเลือกห้องก่อนจะเด้งกลับหน้าแรกแทนที่จะพัง

### กล่องโต้ตอบเข้าถึงได้

- ปิดด้วย `Esc` ได้ (เดิมปิดได้ทางเดียวคือคลิกฉากหลัง)
- `Tab` วนอยู่ในกล่อง ไม่หลุดไปโฟกัสปุ่มด้านหลังที่มองไม่เห็น
- ปิดแล้วโฟกัสกลับไปที่ปุ่มที่เปิดมัน
- ปุ่มย้อนกลับของเครื่องปิดกล่องก่อน ไม่ใช่เปลี่ยนหน้าทั้งหน้า
- ข้อความยืนยันมุมจอประกาศผ่านโปรแกรมอ่านหน้าจอแล้ว (`aria-live`)

### ไม่สลับเวอร์ชันใต้มือระหว่างใช้งาน

service worker เดิมสลับไปใช้โค้ดใหม่ทันทีที่โหลดเสร็จ ซึ่งเกิดขึ้นได้ระหว่างที่
ครูกำลังกรอกคะแนนอยู่ ตอนนี้รอไว้แล้วขึ้นแถบ "มีเวอร์ชันใหม่" ให้กดรีเฟรชเอง

### ไอคอนและตัวตนของแอป

- มี favicon ครบทั้ง 3 หน้าที่เสิร์ฟจริง (root, `docs/`, ฝั่ง Apps Script)
- ไอคอน PWA เป็น PNG จริง — เดิมมีแต่ SVG ซึ่ง iOS ไม่รองรับ ครูที่กด
  "เพิ่มลงหน้าจอโฮม" บน iPhone จึงได้ภาพสกรีนช็อตของหน้าเว็บมาเป็นไอคอน
- แชร์ลิงก์ในไลน์แล้วขึ้นเป็นการ์ดมีรูป (`og:image`)
- สีแถบบนของมือถือตรงกับแถบหัวแอปแล้ว — เดิมประกาศไว้ 3 ที่ ไม่ตรงกันสักที่

### รู้ตัวเมื่อแอปพังที่เครื่องครู

หน้าจอ "เปิดแอปไม่สำเร็จ" มีปุ่มคัดลอกรายงาน (รวมเวอร์ชัน เบราว์เซอร์
จำนวนงานค้างในคิว) และส่งขึ้นชีตให้เองเงียบ ๆ ที่แท็บ `🐞 บันทึกปัญหา`
แท็บนี้ถูกสร้างต่อเมื่อมีปัญหาเกิดขึ้นจริงเท่านั้น

### เครื่องมือและการตรวจ

- มี `package.json` แล้ว — `npm test` / `npm run lint` / `npm run build`
- ESLint ตรวจโค้ดทั้ง 3 สภาพแวดล้อม (เบราว์เซอร์ · Node · Apps Script)
  และ CI รันให้ทุกครั้งที่ push
- `test/queue.mjs` ใหม่ — 26 ข้อ คุมคิวงานออฟไลน์ ซึ่งเป็นตรรกะที่พังแล้ว
  "คะแนนที่ครูกรอกหายเงียบ ๆ"
- ตัวตรวจเวอร์ชันดู `package.json` ด้วย ไม่ใช่แค่ `js/version.js` กับ `sw.js`

### แก้บั๊ก

- **ไฟล์ไม่เคยถูกสำรองอัตโนมัติเลย** ตั้งแต่วันที่เพิ่มฟีเจอร์นี้ — ชื่อฟังก์ชันที่ให้
  ทริกเกอร์เรียกคือ `scheduledBackup_` ซึ่งลงท้ายด้วย `_` Apps Script ถือว่าเป็น
  ฟังก์ชันส่วนตัว ทริกเกอร์เรียกไม่ได้ ตั้งกี่ครั้งก็ไม่ติด · เปลี่ยนเป็น `scheduledBackup`
- **เมนูติดตั้งบอกว่า "เรียบร้อย" เสมอ** แม้ตั้งสำรองไฟล์ไม่สำเร็จ — เป็นเหตุที่ทำให้
  บั๊กข้างบนไม่ถูกเจอมานาน · ตอนนี้ไปตรวจรายชื่อทริกเกอร์จริงแล้วรายงานตามนั้น
  พร้อมแสดงข้อความ error จริงถ้าล้ม
- **ตัวอย่าง `assets_url` ชี้ผิดโฟลเดอร์** ทำให้เปิดลิงก์ `/exec` แล้วเจอ
  "โหลดไฟล์โค้ดจาก GitHub ไม่สำเร็จ" · เปลี่ยนเป็นอธิบายเกณฑ์ให้ครอบคลุมทั้งสองแบบ
- **แท็บ `📖 วิธีใช้` ทำแถวธรรมดาเป็นหัวข้อ** — regex คัดหัวข้อใช้เซ็ตอักขระ
  ที่มีอีโมจิโดยไม่มี flag `u` อีโมจิหนึ่งตัวจึงถูกแยกเป็น surrogate สองตัว
  แล้วไปตรงกับอีโมจิตัวอื่นที่ใช้ตัวหน้าร่วมกัน (📖 📗 📝 ล้วนใช้ `\uD83D`)
- ลบโค้ดตายที่ไม่มีใครเรียกใน `report.js` · `setup.js` · `work.js` · `student/app.js`

---

## ก่อน 3.2.0

ไฟล์นี้เริ่มเขียนที่เวอร์ชัน 3.2.0 — ประวัติก่อนหน้านั้นดูได้จาก

```bash
git log --oneline
```
